import React from 'react';

const PhotoList = () => {
    return (
        <div>
            {photos.map(() => {
                //map over photos to display cards
            })}
        </div>
    )
}